require([ 'datastructures/array', 'datastructures/dynamicArray' ], function ( array, dynamicArray ) {

  // do stuff with stuff :)

  console.log( array.init( 100 ) );

  console.log( dynamicArray.init( 100 ) );

});
